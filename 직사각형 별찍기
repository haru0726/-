process.stdin.setEncoding('utf8');
process.stdin.on('data', data => {
    const dimensions = data.split(" ");
    const n = Number(dimensions[0]);
    const m = Number(dimensions[1]); 

    for (let i = 0; i < m; i++) {
        console.log('*'.repeat(n)); 
    }
});
